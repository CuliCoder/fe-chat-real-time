export const list_conversation = "list_conversation";
export const list_Conversation = (data) => {
  return {
    type: list_conversation,
    data
  };
};
export const get_conversation = (id)=>{
  
}

